from vpython import *
from time import *
#ball = sphere(color = color.green)
#sleep(5)
#ball.color = color.blue
#sleep(5)
#ball.color = color.red
myBox = box(color = color.yellow,length = 12,width = 1,height = .2)
myBox.pos = vector(0,-5,0)
while True:
    pass   # This loop is needed for the program to run,
           # but kill it before restarting